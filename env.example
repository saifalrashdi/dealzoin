# Copy to .env on your server and fill in. server.js reads real env vars; this file is documentation.
PORT=3000
# Change BOTH before going live, then log in and change the admin password from the dashboard.
ADMIN_EMAIL=admin@dealzoin.com
ADMIN_PASSWORD=Dealzoin@2026
# Any long random string in production (keeps sessions valid across restarts):
SESSION_SECRET=
